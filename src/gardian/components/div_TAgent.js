import React, {useEffect, useState} from "react";

export const showAgent = (props) => {
    const [divs, setDivs] = useState(null);

    useEffect(() => {

      //  displayAgents(props.data);

    }, []);



    return (
        <>
           <span>HI</span>
        </>

    );
}
